<div class="row justify-content-center">
	<div class="col-12 col-md-8">
		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn btn-block btn-success">{{ button_label|default('Responder') }}</button>
		{{ form_end(form) }}
	</div>
</div>
